# Loaded for every environment.
# Needed because a stray ~/node_modules on the build machine trips CRA's
# dependency preflight check. Safe: it only disables that version check.
SKIP_PREFLIGHT_CHECK=true
